import user from './user';
import group from './group';
import message from './message';
import usergroup from './usergroup';

const controllers = { user, group, message, usergroup };
export default controllers;
